# Makefile Flag
## CONFIG=debug, CONFIG=release
** While using CONFIG=debug, the DEBUG(msg,...) will be enable and can be debug by gdb,
** Default is CONFIG=release

## DISABLE_MEASURE=true
** While using this macro, all measure timestamp and latency code will be disable